// Injected at build time by Vite's `define` (see vite.config.ts).
declare const __APP_VERSION__: string // human-facing version from package.json (e.g. "1.0.0")
declare const __BUILD_ID__: string // unique per deploy (commit SHA), for update detection
